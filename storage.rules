rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Ficheiros bíblicos públicos para download on-demand
    match /bibles/{fileName} {
      allow read: if true;
      allow write: if false;
    }

    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
